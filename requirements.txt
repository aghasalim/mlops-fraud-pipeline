fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
lightgbm>=4.3
scikit-learn>=1.4
scipy>=1.12
pandas>=2.1
joblib>=1.3
mlflow>=2.12
streamlit>=1.36
httpx>=0.27
# Imported directly in src/ (arrays, RNG), so it is declared rather than
# relied on arriving transitively via another package's pin.
numpy>=1.26
